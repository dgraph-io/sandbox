type Author {
  id: ID!
  name: String
  reputation: Float
  bio: String @lambda
}

type Mutation {
    newAuthor(name: String!, reputation: Float!): Author! @lambda
}

type Query {
    authorsByName(name: String!): [Author] @lambda
}